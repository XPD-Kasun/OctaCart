// @ts-check

/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
    experimental: {
        turbotrace: {}
    },
    output: standalone
    /* config options here */
}

export default nextConfig;