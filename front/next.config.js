// @ts-check

/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
    output: 'standalone',
    transpilePackages: ['@swc/helpers'],
    /* config options here */
}

export default nextConfig;